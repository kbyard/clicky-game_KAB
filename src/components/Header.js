import React from "react";

const Header = () => (
    <div className="jumbotron p-3">
        <h1 className="text-center">Click The Doctor!</h1>
        <h3 className="text-center">Click on an image of The Doctor to earn points, but don't click on any incarnation more than once!</h3>
    </div>
)

export default Header;